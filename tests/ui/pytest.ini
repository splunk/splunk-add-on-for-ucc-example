[pytest]

addopts = -v
    --splunk-type=external
    --splunk-host=10.202.33.181
    --splunk-password=5up3rn0va
    --browser=firefox
    --local
    --persist-browser
    -k alert_actions

markers =
	input: Input Page UI test cases
	account: Account page UI test cases
	proxy:
	logging:
	custom:
	demo:
